#include <string>
#include <iostream.h>
#include <teste>

char a, b[12], c;
double c;

int f( int a, int b  );

void foo(int y[5], int x)
{
  char y[9];

  printf( "%d", y );
}

int main ()
{
  int a;
  char s[2560];
  char* temp_charptr_1; 

  temp_charptr_1 = s + 256;
  strncpy( temp_charptr_1, "hello, world", 256 );  

  temp = x * b;
  a = temp + 3;
  
  
  temp = temp + a;
  b[9] = temp;
  b(1,2,3,temp);
  a = ! temp;
  if( a ) goto i10;
  scanf( "%d", &a ); 
  printf( "Teste", 0 );
i10: return 0;
}
